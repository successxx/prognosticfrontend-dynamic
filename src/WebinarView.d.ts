declare module "./WebinarView.jsx" {
  // if you want to define any types, you can do it here, e.g.:
  // export interface SomeProps { ... }
}

// By including this empty export, TS now sees "it's a module."
export {};
